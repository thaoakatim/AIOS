# ==============================================================================
# AIOS Infrastructure & Application Environment Variables
# Copy file này thành .env hoặc copy vào apps/api/.env để sử dụng:
#   cp docker/.env.example docker/.env
# ==============================================================================

# ------------------------------------------------------------------------------
# 1. POSTGRESQL (pgvector) CONFIGURATION
# ------------------------------------------------------------------------------
POSTGRES_USER=aios_user
POSTGRES_PASSWORD=aios_password
POSTGRES_DB=aios_db
POSTGRES_PORT=5432

# URL kết nối cơ sở dữ liệu cho Prisma ORM (tại apps/api)
DATABASE_URL=postgresql://aios_user:aios_password@localhost:5432/aios_db?schema=public

# ------------------------------------------------------------------------------
# 2. REDIS (Cache & BullMQ Queue) CONFIGURATION
# ------------------------------------------------------------------------------
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=aios_redis_secret

# URL kết nối Redis cho NestJS / BullMQ
REDIS_URL=redis://:aios_redis_secret@localhost:6379

# ------------------------------------------------------------------------------
# 3. QDRANT (Vector Database) CONFIGURATION
# ------------------------------------------------------------------------------
QDRANT_REST_PORT=6333
QDRANT_GRPC_PORT=6334
QDRANT_API_KEY=
QDRANT_URL=http://localhost:6333

# ------------------------------------------------------------------------------
# 4. OLLAMA (Local LLM Provider - Profile: ollama)
# ------------------------------------------------------------------------------
OLLAMA_PORT=11434
OLLAMA_BASE_URL=http://localhost:11434

# ------------------------------------------------------------------------------
# 5. DEV TOOLS PORTS (Profile: tools)
# ------------------------------------------------------------------------------
ADMINER_PORT=8080
REDIS_COMMANDER_PORT=8081

# ------------------------------------------------------------------------------
# 6. LOCAL STORAGE PATH
# ------------------------------------------------------------------------------
LOCAL_STORAGE_PATH=./data/storage

